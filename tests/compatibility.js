TODO tests...it handles

{{nested.data.from.context}}
{{backet[notation][like][this]}}

"a string file path to import structure"

conditionals:
	"{{#if some.data}}": {tagName: {attributes}}

loops:
	"{{#each some.data}}": {tagName: {attributes}} //<- via local context from array

	{{{allows.unescaped.text}}}